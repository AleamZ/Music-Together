# Handoff — feat/v15-field, 2026-09-26

## State
`feat/v15-field` contains the following, not merged to `main`:
- v15.1 (0013)
- anti-cheat (0015)
- v15.2 (0016)
- v16 cards (0017)
- v15.3 gather (0018)

Each of these was reviewed, with fix rounds done. Plans are in `docs/superpowers/plans/`, specs in `docs/superpowers/specs/`.

## Next: v17 (rats, dog, slingshot — `0019_v17_rats.sql`)
- **Plan:** `docs/superpowers/plans/2026-09-26-music-together-v17.md`.
- **Validation:** replay-validated on `793f8b4` (1 409 tests, build OK, SQL chain ALL OK).
- **Existing implementation:** the whole plan is already on branch `wip/v17-plan` (head `7e9f077`, one commit per task, based on `793f8b4`).
  - If that branch is pushed, fast-forward it onto `feat/v15-field` instead of re-implementing it.
  - If `feat/v15-field` has moved past `793f8b4`, cherry-pick the commits.
- **Still to do after adopting:** an SQL area review (money and locks: reproduce on a throwaway Postgres), a client review, a fix round, then a final full check (vitest, tsc, SQL chain, `pnpm build`).
- **Plan-writer rulings the owner should confirm:**
  - `DogPanel` lives in `components/game/farm/`.
  - For the auto-hunt, "not locked" means no farm job or SlingGame is open.
  - `presenceDog` accepts spaces and symbols.
  - A sling hit is judged at the aim on release.

## Rules
- Never run anything against the hosted Supabase DB. The owner runs migrations in the SQL Editor.
- For SQL checks, use a throwaway local cluster (`initdb --auth=trust`, a non-5432 port). Each plan's Global Constraints carry the check script.
- Never merge to `main` or push without the owner's word. A v15+ client on `main` before the migrations run breaks production.
- The repo is public: no secrets, original art only, no literal invisible characters (use escapes).
- Talk to the owner in Vietnamese, tersely.

## Migrations
The owner runs them in this order: `0012` → `0014` (already on main) → `0013` → `0015` → `0016` → `0017` → `0018` → `0019`.
- Before `0015`, run the two pre-deploy queries in anti-cheat spec §11.4.
- Each release's deploy order (migration first or client first) is in the README.
- Re-running an older migration after newer ones: see anti-cheat spec §11.3, rule 7.
